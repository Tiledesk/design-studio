import { GetVariableNamePipe } from './get-variable-name.pipe';

describe('GetVariableNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetVariableNamePipe();
    expect(pipe).toBeTruthy();
  });
});
